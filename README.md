# cake-yoyaku-web

オーダーケーキ予約システムの配信用リポジトリ（GitHub Pages）。
**原本は `oyatsupokke/cake-yoyaku-biz`（非公開）の `web/`。編集はあちらで行い、このリポジトリへは同期スクリプトでコピーする。ここを直接編集しない。**

- 顧客フォーム: `index.html`（`?shop=pokke` で店舗指定・省略時pokke）
- 管理画面: `admin/`（要ログイン。データはSupabase RLSで保護）
